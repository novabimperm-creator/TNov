<Window x:Class="TNov.CopyWindowsWPF"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TNov"
             mc:Ignorable="d" Width="650" Height="500"
            WindowStartupLocation="CenterScreen" 
            Background="White" BorderBrush="#FF008E43" BorderThickness="1"
            ResizeMode="NoResize" WindowStyle="None">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="booleanToVisibilityConverter"/>

        <!-- Стиль с полным шаблоном -->
        <Style x:Key="CustomButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <!-- Убираем SystemColors -->
                        <ControlTemplate.Resources>
                            <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" 
                                         Color="Transparent"/>
                            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" 
                                         Color="Transparent"/>
                        </ControlTemplate.Resources>

                        <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <!-- Прямое указание цвета -->
                                <Setter TargetName="border" 
                                    Property="Background" 
                                    Value="#FF008E43"/>
                                <!-- Цвет заголовка -->
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" 
                                    Property="Background" 
                                    Value="#FF008E43"/>
                                <!-- Темнее -->
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" 
                                    Property="Opacity" 
                                    Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="30"
              ResizeBorderThickness="5"
              GlassFrameThickness="0"
              CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Grid ShowGridLines="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="30 px"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <StackPanel VerticalAlignment="Center"  Grid.Row="0"  >
            <Border Height="30" 
        Background="#FF008E43" 
        VerticalAlignment="Top"
        MouseDown="Border_MouseDown">
                <TextBlock Text="TNov - Проемщик" 
               Foreground="White"
               VerticalAlignment="Center"
               Margin="10,0"/>
            </Border>
        </StackPanel>
        
        <StackPanel VerticalAlignment="Center" Margin="5" Grid.Row="1">
            <TextBlock Text="Отмеченные элементы будут скопированы (старые удалены)" Margin="5"/>

        </StackPanel>

        <!-- Панель фильтрации по категории -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5">
            <TextBlock Text="Показать:" VerticalAlignment="Center" Margin="5"/>
            <ComboBox x:Name="CategoryFilterCombo" Width="120" SelectionChanged="CategoryFilter_Changed">
                <ComboBoxItem IsSelected="True">Все</ComboBoxItem>
                <ComboBoxItem>Окна</ComboBoxItem>
                <ComboBoxItem>Двери</ComboBoxItem>
            </ComboBox>
        </StackPanel>
        <!-- Список элементов -->
        <ListView Grid.Row="3" Name="ItemsListView" ItemsSource="{Binding}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Выбрать" Width="60">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected}" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Категория" Width="120" DisplayMemberBinding="{Binding Category}"/>
                    <GridViewColumn Header="Тип" Width="200" DisplayMemberBinding="{Binding TypeName}"/>
                    <GridViewColumn Header="Высота (мм)" Width="100" DisplayMemberBinding="{Binding HeightMm, StringFormat={}{0:F0}}"/>
                    <GridViewColumn Header="Ширина (мм)" Width="100" DisplayMemberBinding="{Binding WidthMm, StringFormat={}{0:F0}}"/>
                </GridView>
            </ListView.View>
            <ListView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" Background="LightGray" Padding="5"/>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>
        </ListView>
        <Grid Grid.Row="4" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Ввод глубины -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                <TextBlock Text="Глубина проема (мм):" VerticalAlignment="Center" Margin="5"/>
                <TextBox x:Name="DepthTextBox" Width="100" Text="500" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Кнопки -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource CustomButtonStyle}" Content="Закрыть" x:Name="escButton" Click="escButton_Click" MinWidth="90" MinHeight="25" Margin="5" />
                <Button Style="{StaticResource CustomButtonStyle}" Content="Выбрать все" Click="SelectAll_Click" Width="90" MinHeight="25" Margin="5"/>
                <Button Style="{StaticResource CustomButtonStyle}" Content="Снять все" Click="DeselectAll_Click" Width="90" MinHeight="25" Margin="5"/>
                <Button Style="{StaticResource CustomButtonStyle}" Content="Создать" Click="Create_Click" Width="90" MinHeight="25" Margin="5"/>
            </StackPanel>
        </Grid>
    </Grid>

</Window>
